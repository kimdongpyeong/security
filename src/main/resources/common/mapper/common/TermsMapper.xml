<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.supporti.api.common.mapper.TermsMapper">

    <sql id="selectFrom">
        SELECT
            `terms`.`id`,
            `terms`.`title`,
            `terms`.`contents`,
            `terms`.`publish_yn`,
            `terms`.`required_yn`,
            `terms`.`created_date`,
            `terms`.`last_modified_date`
        FROM
            `tb_terms` `terms`
    </sql>

    <sql id="where">
        <where>
            <if test="termsParamDto.adminYn != null and termsParamDto.adminYn != 1">
                AND
                    `terms`.`publish_yn` = 'Y'
               ORDER BY
                   `terms`.`required_yn`
            </if>
        </where>
    </sql>

    <resultMap id="termsResultMap" type="kr.supporti.api.common.entity.TermsEntity">
        <result property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="contents" column="contents"/>
        <result property="publishYn" column="publish_yn"/>
        <result property="requiredYn" column="required_yn"/>
        <result property="createdDate" column="created_date"/>
        <result property="lastModifiedDate" column="last_modified_date"/>
    </resultMap>

    <select
        id="selectTermsList"
        parameterType="java.util.Map"
        resultMap="termsResultMap"
    >
<!--         <include refid="PageFragment.sortStart"/> -->
        <include refid="selectFrom"/>
        <include refid="where"/>
<!--         <include refid="PageFragment.sortEnd"/> -->
<!--         <include refid="PageFragment.limitOffset"/> -->
    </select>

    <select
        id="selectTermsListCount"
        parameterType="java.util.Map"
        resultType="java.lang.Integer"
    >
        <include refid="PageFragment.countStart"/>
        <include refid="selectFrom"/>
        <include refid="where"/>
        <include refid="PageFragment.countEnd"/>
    </select>

    <select
        id="selectTerms"
        parameterType="java.util.Map"
        resultMap="termsResultMap"
    >
        <include refid="selectFrom"/>
        WHERE
            `terms`.`id` = #{id}
    </select>


</mapper>